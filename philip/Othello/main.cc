
#include "othello.h"
#include "board.h"
#include "game.h"
#include "piece.h"
#include <iostream>

using namespace std;
using namespace main_savitch_14;

int main()
{
  //bool run = 1;
  //char c;
  othello my_game;
  //do{
    my_game.play();
    my_game.game_end();
    //cout << "Play another Game? (y) >";
    //cin >> c;
    //cout << endl;
    //if(char(c) == 'Y' || char(c) == 'y')
    //  run = 1;
  //  else
    //  run  = 0;
    //  }while(run);
}
